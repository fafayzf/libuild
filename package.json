{
  "name": "yzfu-libundler",
  "version": "1.0.4",
  "description": "rollup build",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/fafayzf/libuild"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "scripts": {
    "build": "pnpm -r --filter=./packages/** run build",
    "pub": "pnpm build && bumpp -r && pnpm publish -r"
  },
  "keywords": [],
  "author": "fafayzf",
  "license": "MIT",
  "devDependencies": {
    "@types/babel__core": "^7.20.1",
    "@types/node": "^20.4.6",
    "bumpp": "^9.1.1"
  }
}
